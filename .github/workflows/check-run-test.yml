name: Check Run Neutral Test

on:
  push:
  workflow_dispatch:

jobs:
  neutral_job:
    runs-on: ubuntu-latest
    steps:
      - name: Emit warning but succeed
        run: |
          echo "::warning::This is a neutral outcome"
          exit 0
