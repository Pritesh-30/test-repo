name: Check Run Cancelled Test

on:
  push:
  workflow_dispatch:

jobs:
  cancelled_job:
    runs-on: ubuntu-latest
    steps:
      - name: Long running step (cancel me)
        run: |
          echo "Sleeping... cancel this job from UI"
          sleep 600
